name: E2E

# Gates a PR on the golden-path E2E suite (e2e/) running against a production
# build on the runner. This app has no database, auth, or serverless
# functions to diverge from a real deployment — it's a static marketing
# page — so a plain `next build && next start` is a faithful stand-in and
# needs no Vercel secrets at all.

on:
  pull_request:
    branches: [main]

jobs:
  lint:
    # Independently gated so a lint regression can't merge just because it
    # doesn't happen to break the build (see issue #30).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm ci

      - run: npm run lint

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm ci

      - run: npm run build

      - name: Start server
        # Output redirected to a file rather than left to inherit the step's
        # own stdout: GitHub Actions stops capturing a backgrounded (`&`)
        # process's output once the step that launched it exits. "Print
        # server log" below cats this file so it's visible regardless of
        # outcome.
        run: |
          npm run start > /tmp/server.log 2>&1 &
          for i in $(seq 1 30); do
            curl -sf http://localhost:3000/ > /dev/null && exit 0
            sleep 1
          done
          echo "Server did not become ready in time" >&2
          exit 1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Playwright (Python)
        run: |
          pip install playwright
          playwright install --with-deps chromium

      - name: Run E2E suite
        run: python e2e/run.py

      - name: Print server log
        if: always()
        run: cat /tmp/server.log
